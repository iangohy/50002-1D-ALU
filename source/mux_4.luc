module mux_4 (
    input s[2], // selector signals
    input a,
    input b,
    input c,
    input d,
    output out
  ) {

  always {
    out = 0;
    case(s) {
      b00:
        out = a;
      b10:
        out = b;  
      b01:
        out = c;
      b11: 
        out = d;
    }
  }
}